require 'spec_helper'

describe Itamae::Plugin::Resource::PostgresqlUser do
  it 'has a version number' do
    expect(Itamae::Plugin::Resource::PostgresqlUser::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
